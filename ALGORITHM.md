# 工作流生成算法说明（Algorithm）

## 1. 目标与输入输出

### 1.1 目标
- 将用户输入（自然语言 + 上传文件）转化为可执行、可回滚、可持久化的工作流更新。
- 在出现冲突或不确定时，优先保证现有流程稳定，不做高风险破坏性修改。

### 1.2 输入
- 用户消息：自由文本（可能是闲聊，也可能是流程修改请求）。
- 上传文件：`pdf/docx/txt`，支持多文件。
- 当前基线：`workflows.json` 中已存在工作流。

### 1.3 输出
- 意图分类结果：`chat` 或 `workflow_edit`。
- 原子操作列表（带 `reason`）：
  - `INSERT_NODE`
  - `DELETE_NODE`
  - `UPDATE_NODE`
  - `ADD_BRANCH`
  - `REMOVE_BRANCH`
  - `ADD_WORKFLOW`
- 变更后的工作流、高亮节点、跳过操作列表。

## 2. 数据结构约束（强校验）

### 2.1 Workflow
- `Record<fileType, { nodes: Node[], edges: Edge[] }>`

### 2.2 Node（必须字段）
- `id: string`（稳定、可读、英文 slug）
- `title: string`（人类可读步骤名）
- `office: string`
- `role: string`
- `materials: string[]`
- `note: string`
- `extendable_fields: object`

### 2.3 Edge（必须字段）
- `from: string`
- `to: string`
- `condition: string | null`

## 3. 主流程（端到端）

1. **文本提取与标准化**
   - 文件分别提取文本，合并为统一上下文。
   - 超长文本按上限截断，避免模型超长失败。

2. **意图分类**
   - 先判断是普通对话还是工作流编辑意图。
   - `chat` 直接返回文本回复，不改工作流。
   - `workflow_edit` 进入后续两阶段流程。

3. **阶段一：类型识别**
   - 识别 `existingTypes` 与 `newTypes`。
   - 若有 `newTypes`，且未确认：只返回候选，不落库。

4. **阶段二：按类型产出操作**
   - 对每个目标类型生成原子操作列表。
   - 统一补全元数据（如缺失 `reason/fileType`）。

5. **操作校验与应用**
   - 按操作类型做 payload 校验。
   - 不合法操作写入 `skippedOperations`，不执行。
   - 执行后进行完整性修复（节点字段、边去重、端点补齐节点）。

6. **持久化与会话记录**
   - 写入 `workflows.json`。
   - 保存 `latestSession`（含 baseline、operations、highlights）。
   - 支持按操作 ID 单条撤回（基于 baseline 重算）。

## 4. 冲突处理算法（重点）

### 4.1 冲突定义
- **事实冲突**：新输入与现有节点字段语义冲突（例如 office/role 相互矛盾）。
- **结构冲突**：新边会导致不合理断裂（删除关键节点、悬挂边、重复冲突分支）。
- **命名冲突**：新增节点与已有节点语义重复但 id 不同。

### 4.2 冲突处理优先级
1. **数据完整性优先**：任何会破坏图可用性的操作先拒绝或降级。
2. **非破坏优先**：优先 `UPDATE/ADD`，谨慎执行 `DELETE/REMOVE`。
3. **显式证据优先**：文件直接证据 > 用户简短口语 > 模型猜测。
4. **保守回退优先**：不确定时跳过并记录 `skippedOperations`，不强行改。

### 4.3 冲突决策规则
- `UPDATE_NODE` 与现值冲突：
  - 若新证据强（文件明确条款/明确指令），执行更新，并在 `reason` 说明依据。
  - 若证据弱，保留原值，将候选改为备注（写入 `note` 补充）或跳过。
- `DELETE_NODE`：
  - 若节点仍被边引用，先拒绝删除或要求先移除相关边。
  - 默认不执行“低置信删除”。
- `ADD_BRANCH`：
  - 若同 `(from,to,condition)` 已存在，去重，不重复添加。
  - 若 `from/to` 缺失节点，自动补齐最小节点骨架并打 `auto_created` 标记。
- `ADD_WORKFLOW`：
  - 仅在用户确认 `newTypes` 后执行。

## 5. 健壮性增强策略（建议并已部分实现）

### 5.1 LLM 输出鲁棒性
- JSON 提取采用“代码块优先 + 首尾大括号兜底”。
- 首次解析失败时，触发 repair prompt 二次修复。
- Provider 级重试与退避，Kimi 模型 fallback。

### 5.2 响应鲁棒性
- 关键写入与辅助文案解耦：写入成功后，即使总结失败也返回成功（避免假失败）。
- 前端统一安全 JSON 解析，拦截 HTML/非 JSON 返回。

### 5.3 图完整性鲁棒性
- 节点字段自动补齐，防止 `undefined.id` 一类崩溃。
- 边端点无节点时自动建占位节点，避免空画布。
- 每次写入前后进行 normalize，确保 schema 一致。

## 6. Corner Cases 清单与处理

1. **多文件混合多个类型**
   - 先类型聚类，再按类型独立生成操作，避免相互污染。

2. **同一批输入里自相矛盾**
   - 以“最近且明确证据”为主；无法判定时跳过冲突操作并提示用户复核。

3. **模型返回自然语言而非 JSON**
   - 进入 repair prompt；仍失败则返回可读错误，不落库。

4. **新增节点仅有边无元数据**
   - 自动生成骨架节点（可编辑），并在前端高亮/标记。

5. **用户只上传文件不输入文字**
   - 使用默认分析提示语，流程照常执行。

6. **删除导致图断裂**
   - 删除操作降级为 skipped，并反馈原因。

7. **部分操作有效、部分无效**
   - 成功部分照常应用；失败部分进入 `skippedOperations`，不中断全局。

8. **AI 结果成功但回复文案失败**
   - 返回兜底成功消息，禁止影响前端状态更新。

## 7. 建议新增的进一步增强（下一步可落地）

1. 引入 `confidence` 字段（每条操作 0-1），低于阈值的破坏性操作默认跳过。
2. 为 `DELETE_NODE/REMOVE_BRANCH` 增加“二次确认队列”（先展示待确认，再执行）。
3. 增加“冲突报告 API”，前端可展示“检测到冲突，采用了保守策略”。
4. 为 `workflows.json` 增加版本号和变更摘要，便于追踪回滚。

## 8. 维护规则

- 当以下任一项改变时，必须同步更新本文件：
  - 原子操作定义
  - 冲突处理策略
  - 数据结构字段
  - 容错/回退逻辑
- `SPEC.md` 描述“当前功能事实”，本文件描述“算法与决策规则”；两者需保持一致。
